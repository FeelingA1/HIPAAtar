import React, { useState } from "react";

const Goal = (props) => {
    return (
        <>
            <div>
                <p>{props.goalTitle}</p>
            </div>
        </>   
    );
}

export default Goal;